# Changelog

## 3.13.0-alpha.2 - 2026-08-24（预发布）

- 完整继承 Alpha 1 的高清/动态记忆大厅、背景架构与性能优化、素材质量隔离，以及 Issue #13 的整页横向偏移修复。
- 修正 Windows 云端验收对响应式布局的错误假设：主功能回归现在固定使用 1920×1080 桌面视口，不再把窄屏下正常隐藏的个人进度卡误判为启动失败。
- 动态背景回归改用测试现场生成的 1920×1080 原图与 1280×720 预览，不再用正方形应用图标冒充宽屏大厅，保证智能裁切结果可重复。
- 本机源代码模式与 Windows 解包版均通过素材隔离、2560×1600 多年份布局、三种图片适配、WebM 播放、主功能和中英文界面验收。

## 3.13.0-alpha.1 - 2026-08-24（构建未发布）

- 记忆大厅新增“智能填充、全屏裁切、完整显示”三种画面模式；智能模式会根据素材与窗口比例自动选择无黑边裁切或柔化延展，沉浸模式保持铺满窗口。
- 支持最高 160 MB 的高清静态背景与最高 2 GB 的 MP4 / WebM 动态大厅；动态素材可调 0.5×～1.5× 播放速度，并可在窗口失焦、隐藏或启用“减少动态效果”时自动暂停。
- 自定义高清图导入时自动保留原图并生成 1280px 轻量预览；旧素材库在真正显示某张图时按需生成预览并缓存，避免启动时批量处理和后台虚化层重复读取高清原图。
- 背景渲染、单图导入和本地素材库分别收拢为独立模块；素材格式、容量、元数据与预览策略均集中配置，后续增加图片或视频格式更容易维护。
- 本地素材清单增加修改时间缓存，常规渲染使用壁纸索引映射；背景等次要数据更早在空闲阶段加载，同时保留首屏题库优先策略。
- 修复拆包图层被误当作完整大厅的问题：已知碎片素材、清单中标记为 `layer/sprite/thumbnail/transition` 的非壁纸资源会在素材服务入口统一隔离；若旧配置正指向坏素材，会自动回退到安全大厅。
- 修复 Issue #13：同步后出现多年份热力图时，只滚动年份选择条，不再带动整个应用横向偏移或遮住左侧导航。
- 新增高清原图/预览分离、三种适配模式、沉浸覆盖、WebM 真解码、变速播放、动态节能暂停、素材隔离回退和路径安全回归；主功能、模板、赛事、训练分析与双语首启均通过隔离验收。
- 该标签的 Windows 打包成功，但云端主功能验收受窄屏响应式测试假设影响而中止，因此没有生成 GitHub Release，也没有关闭 Issue；修正版由 Alpha 2 接替。

## 3.12.3-rc.1 - 2026-08-24（仅本地验收）

- 自定义静态背景改为按本地文件地址加载，避免每次打开外观设置都把高清图片转成体积更大的 Base64 数据；动态背景和既有调用方式保持兼容。
- 将自定义背景的格式、大小限制、导入与清理逻辑收拢为独立服务，后续新增素材格式时只需修改一处配置。
- 最近活动在正常的倒序同步数据上跳过重复排序，外部导入的乱序数据仍自动回退到原排序流程；10 万条提交基准约提升 1.5 倍。
- 题库缓存改为紧凑 JSON 写盘，用户设置、模板配置、备份与手动导出继续保留易读格式。
- 复用数字格式化器，并为背景缩略图启用延迟解码；新增性能与背景兼容回归测试。
- 本版本仅用于本机候选验收，尚未创建 Git 标签、推送提交或发布 GitHub Release。

## 3.12.2 - 2026-08-23

- 首次启动现在跟随系统语言：中文系统默认简体中文，其他系统默认 English；已有用户保存的语言选择保持不变。
- 修复英文界面“今日训练”复习卡片中长标签被挤成单个字母的问题，并正确区分 `1 day` 与复数 `days`。

## 3.12.1 - 2026-08-23

- 修复 Issue #12：macOS Intel 与 Apple Silicon 应用现在使用明确的 ad-hoc 签名，不再发布完全无签名的应用包。
- macOS 构建完成后新增严格的深度签名验证，应用或内部组件签名异常时将直接阻止 Release。
- 完善 macOS Gatekeeper 说明，区分“安装包损坏”与“未经过 Apple Developer ID 公证”，并提供校验后的安全处理步骤。
- Release 流水线现在优先读取版本更新说明，并在所有平台成功发布后自动回复和关闭对应的已解决 Issue。

## 3.12.0 - 2026-08-22

- 新增简体中文 / English 全局切换，覆盖主要页面、弹窗、动态计数、系统对话框与无障碍提示，同时保持题名、模板和用户内容原文。
- 新增个人训练分析页：统一时间范围、Codeforces Rating 分段曲线、滚轮缩放、算法通过率排行和训练节奏统计。
- 重做年度刷题热力图与标签隐藏体验；热力图支持跨年份横向查看，题目标签在窄空间下保持可读。
- 恢复本地完整记忆大厅素材库支持，同时继续将第三方人物与背景素材排除在公开仓库和安装包之外。
- 回应 Issue #10，正式提供 Windows x64、Linux x64、macOS Intel 与 macOS Apple 芯片 Release 包。
- 每个平台安装包均附独立 SHA-256，Release 同时提供完整 `SHA256SUMS.txt` 校验清单。

## 3.11.5 - 2026-08-20

- 将桌面运行时升级至 Electron 43.4.0，并将 electron-builder 升级至 26.15.3，消除旧运行时带来的已知安全风险。
- 加固 Electron 导航、窗口创建、权限请求和 IPC 来源校验，非受信页面无法调用本地能力。
- 修复 Windows 可执行文件的图标与版本元数据；公开构建仍不冒充已签名软件，并继续提供 SHA-256 校验文件。
- 修正 Carrot FFT 预测核心的原始项目链接、作者归属与 MIT 版权声明。
- GitHub Actions 现在会执行依赖安全检查、重点回归测试、渲染构建及完整 Windows portable 打包。
- 新增英文项目说明，并清理测试截图名称和问题模板中的历史版本号。

## 3.11.4 - 2026-08-20

- 开源版外观设置新增本地人物与背景导入入口，支持常见图片及动态素材格式。
- 恢复本地背景的可见度预设、画面参数、九宫格焦点和节能控制。
- 新增可选素材来源与使用指南；第三方素材仍不进入仓库、安装包或 MIT License 范围。
- 新增外观设置与数据中心完整使用手册；演示图已去除电脑路径和私密文件信息。

## 3.11.3 - 2026-08-20

- 重写 README，重点展示题库、今日训练、赛事检索、赛事复盘与本地模板库的完整训练闭环。
- 加入赛事中心、赛事复盘、今日训练、模板库与模板题意的真实演示截图。
- 为 GitHub Release 提供经过启动和界面回归验证的 Windows x64 portable 程序。
- 移除模板库浏览器回退状态中的维护者本机路径。

## 3.11.2 - 2026-08-20

- 首次发布 CF Compass 开源版。
- 保留天空蓝、薄荷绿、珊瑚粉三套原创主题色。
- 移除所有第三方壁纸、角色图片、素材库和本地用户数据。
- 加入模板摘要的统一可读格式规范。
