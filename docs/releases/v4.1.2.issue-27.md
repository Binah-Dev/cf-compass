感谢反馈！已在 [v4.1.2](https://github.com/Binah-Dev/cf-compass/releases/tag/v4.1.2) 修复并完成发布验证。

- 增强复盘保存时会保留源码时间线与 Diff；重新打开/重启后复用报告，不会因旧版明细缺失自动重新请求 AI。保存失败也不再显示成功。
- 推荐题单按题号从同步题库补齐缺失的 Rating/标签，并区分“未同步”“元数据不足”“没有符合条件的未做题”。这里的本地题库是“题库工作台 → 同步数据”，不是模板库。
- 同时修复切换场次后旧请求覆盖新结果的问题。

旧版已经丢失的源码明细无法自动恢复，报告正文仍可查看；如需要明细，请手动重新分析。元数据尚未公开或候选题确实耗尽时，仍会明确提示而不是编造推荐。

已覆盖保存/重启、旧报告、延迟请求及失败保存回归，通过跨平台构建、校验和发布下载、Windows 安装/冷启动验证。AI 回归使用模拟服务，未调用付费接口。先关闭本问题；升级后若仍能复现，欢迎重新打开并补充脱敏错误与操作步骤，请勿上传密钥。
